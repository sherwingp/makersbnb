# frozen_string_literal: true
# require_relative '../model/bookings'

# describe Bookings do
#   describe '.book' do
#     it "make a booking" do
#       connection_test = DatabaseConnection.setup('makersbnb_test')
#       connection.exec("INSERT INTO spaces(id, location, price, host_id, guest_id, booked)
#       VALUES (5,'Shoreditch', '500', 'host_1', '1',);")
#       booking = Bookings.book(id: 5)
#       # expect(booking.values).to include([TRUE])

#       # subject.create_new_user("DummyUsername", "complexPassword")
#       # result = psql.exec "SELECT usename FROM pg_catalog.pg_user"
#       # expect(result.values).to include(["DummyUsername"])
#     end
#   end
# end

# connection.exec("INSERT INTO spaces(id, location, price, host_id, guest_id, booked)
# VALUES (5,'Shoreditch', '500', 'host_1', '1',);")
